# 项目实施状态

更新时间：2026-06-02

## 已完成

- 已读取并整理原始交接文档。
- 已生成用户侧剩余待办：[用户准备事项代办清单.md](用户准备事项代办清单.md)
- 已生成一步到位方案：[部署方案.md](部署方案.md)
- 已生成项目说明：[README.md](README.md)
- 已加入 `.gitignore`，避免提交 `key.md`、`.env`、`.env.local` 等敏感文件。
- 已从 `key.md` 生成本地私有 `.env.local`，未打印密钥。
- 已按当前官方模型信息设置：
  - Chat：`gpt-5.5`
  - Embedding：`text-embedding-3-large`
- 已生成本地脚本：
  - `scripts/local/prepare.ps1`
  - `scripts/local/start.ps1`
  - `scripts/local/verify.ps1`
  - `scripts/local/list-models.ps1`
  - `scripts/local/set-models.ps1`
  - `scripts/local/download-fastgpt-compose.ps1`
  - `scripts/local/generate-fixtures.ps1`
- 已生成服务器部署骨架：
  - `deploy/server/bootstrap.sh`
  - `deploy/server/check-server.sh`
  - `deploy/server/README.md`
  - `deploy/nginx/kb.zero007.chat.conf.template`
- 已生成验收资料：
  - `tests/fixtures/ai-knowledge-overview.md`
  - `tests/fixtures/mcp-agent-notes.txt`
  - `tests/fixtures/acceptance-questions.md`
  - `tests/fixtures/deployment-checklist.docx.txt`
  - `tests/fixtures/citation-policy.pdf.txt`
- 已初始化本地 Git 仓库，默认分支为 `main`。
- 已创建 GitHub 私有仓库：`https://github.com/zeroliao/ai-knowledge-base`
- 已绑定 Git remote：`origin`
- 已完成初始提交并 push 到 GitHub `main` 分支。
- GitHub SSH 22 端口从本机超时，当前仓库 remote 已改为 HTTPS：
  - `https://github.com/zeroliao/ai-knowledge-base.git`
- 已确认 `key.md` 和 `.env.local` 被 `.gitignore` 排除。
- 已确认用户完成 `kb.zero007.chat` Cloudflare 配置。
- 已确认 `kb.zero007.chat` 已切到 `DNS only`，并解析到源站 IP `43.133.231.45`。
- 已确认用户手动启动 Docker Desktop。
- 已确认 Docker daemon 当前可访问。
- 已确认服务器剩余磁盘空间大于 30 GB。
- 已确认服务器快照已创建。
- 已从截图确认腾讯云安全组已开放 TCP `80` 和 `443`。

## 当前阻塞

1. GitHub CLI 已安装但当前 PATH 未刷新。
   - 已找到完整路径：`C:\Program Files\GitHub CLI\gh.exe`。
   - 当前状态：已登录 `zeroliao`，GitHub 私有仓库已创建。

2. FastGPT 官方 Compose 已下载。
   - 路径：`runtime/fastgpt/docker-compose.pg.yml`
   - `config.json` 已下载到 `runtime/fastgpt/config.json`
   - 主应用镜像 `ghcr.io/labring/fastgpt:v4.14.22` 已拉取成功。
   - 启动官方完整 Compose 时，辅助镜像 `ghcr.io/labring/fastgpt-code-sandbox:v4.14.22` 仍出现 layer size validation 失败。
   - V1 暂不需要复杂 Agent sandbox，下一步应生成本地轻量 override 或改用服务器拉取验证。

3. sub2api 模型接口尚未验证通过。
   - Chat 模型 `gpt-5.5` 已验证通过。
   - `key.md` 已加入独立 embedding 通道：`api.xiaomimimo.com/anthropic`。
   - 脚本已支持 Chat 与 Embedding 使用不同 Base URL / API Key。
   - MiMo embedding 通道目前仍未通过 OpenAI-compatible `/v1/embeddings` 验证。
   - FastGPT 知识库向量化必须使用 OpenAI-compatible embeddings endpoint。

4. 当前 Codex 进程未解析到 `ssh sub2api`。
   - 用户本机终端可以使用该别名。
   - 当前环境执行时报 `Could not resolve hostname sub2api`。
   - 后续需要让当前环境读取相同 SSH config，或提供公网 IP/可用 alias。

## 用户下一步

1. 修通 embedding endpoint：
   - 当前 embedding URL 是 `https://api.xiaomimimo.com/anthropic`；
   - FastGPT 需要 `POST /v1/embeddings` 这类 OpenAI-compatible endpoint；
   - 需要确认 MiMo 是否有对应 `/v1` embedding 协议地址，或改用 sub2api 中可转发的 OpenAI-compatible embedding 通道。
2. 让当前 Codex 环境也能解析 `ssh sub2api`，或提供服务器公网 IP/SSH alias 配置方式。

## 继续执行命令

Docker 权限修复后：

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\local\prepare.ps1
powershell -ExecutionPolicy Bypass -File .\scripts\local\list-models.ps1
powershell -ExecutionPolicy Bypass -File .\scripts\local\verify.ps1
powershell -ExecutionPolicy Bypass -File .\scripts\local\start.ps1
```
