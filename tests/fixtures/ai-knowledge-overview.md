# AI 私有知识库 V1 概览

V1 的目标是部署一个个人版 NotebookLM 风格的私有知识库。系统保存原始资料，支持 PDF、DOCX、TXT、Markdown 和手动文本入库，并通过 embedding 检索、关键词检索和混合检索完成 AI 问答。

系统必须展示可理解来源，来源需要能够追溯到原始文件、原图或原始链接。分类不预初始化，只保留兜底分类：待分类 / 临时收集。

动态分类流程是在录入时提取标题、摘要、关键词和已有分类，再由 AI 推荐使用已有分类、新增分类或进入待分类，用户确认后入库。
