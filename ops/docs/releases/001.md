# v001 URL 目录导入上线

## 范围

- 状态：成功
- 类型：混合
- 触发人：用户
- 分支：main / release/001
- 初始 main commit：52f7025
- FastGPT commit：cee26b5
- release commit：52f7025 + FastGPT cee26b5
- tag：v001
- 回滚目标：服务器当前 `ghcr.io/labring/fastgpt:v4.14.22` 镜像和 `/opt/fastgpt/docker-compose.server.override.yml` 备份

## 变更

- 在 FastGPT 知识库导入入口新增 `URL 目录`来源。
- 支持从入口地址发现 URL：站点页、robots.txt、sitemap、sitemap index、RSS、Atom、HTML 同站链接。
- URL 目录导入复用 FastGPT 原有链接集合创建和训练流程。
- 上传确认页将 `开始上传`按钮放到内容滚动区外，避免长列表滚到底才能操作。
- 模型渠道、Embedding 模型、Base URL 和 API Key 继续只通过 FastGPT 原有页面配置。

## 验证

- FastGPT：`pnpm --filter @fastgpt/app typecheck` 通过。
- FastGPT：i18n JSON 解析通过。
- FastGPT：`git diff --check` 通过，仅有 Windows 行尾提示。
- 本地页面：URL 目录导入 613 条链接，训练状态达到 `已就绪`。

## 服务器记录

- 快照：用户已在腾讯云完成实例回滚；本次部署前以官方镜像 `ghcr.io/labring/fastgpt:v4.14.22` 和 override 备份作为明确回滚点。
- 部署前资源状态：内存 3.6GiB，总可用约 1.9GiB；swap 2.0GiB，可用约 852MiB；根分区 59G，剩余 39G。
- 部署前容器状态：`fastgpt-app ghcr.io/labring/fastgpt:v4.14.22 Up 2 days`；FastGPT 依赖容器和 Caddy 均正常。
- 部署命令：
  - 本地构建：`docker build -f projects/app/Dockerfile -t fastgpt-custom:url-directory-001 .`
  - 本地导出：`docker save fastgpt-custom:url-directory-001 -o fastgpt-custom-url-directory-001.tar`
  - 上传镜像：`scp fastgpt-custom-url-directory-001.tar kb-server:/tmp/fastgpt-custom-url-directory-001.tar`
  - 服务器加载：`sudo -n docker load -i /tmp/fastgpt-custom-url-directory-001.tar`
  - override 备份：`/opt/fastgpt/docker-compose.server.override.yml.bak.20260606015941`
  - compose 校验：`sudo -n docker compose --env-file .env -f docker-compose.pg.yml -f docker-compose.server.override.yml config`
  - 重启 app：`sudo -n docker compose --env-file .env -f docker-compose.pg.yml -f docker-compose.server.override.yml up -d fastgpt-app`
- 部署结果：`fastgpt-app` 已使用 `fastgpt-custom:url-directory-001` 启动。
- 访问验证：`http://127.0.0.1:3000` 返回 200；`https://kb.zero007.chat` 返回 200。
- 日志检查：FastGPT 启动完成，MongoDB/Postgres/队列 worker 正常；未见 fatal/error。保留已知提示：Redis 当前为 `allkeys-lru`，sandbox 禁用导致 `fastgpt-code-sandbox` 检查警告。

## 备注

- 本版本仅部署 FastGPT app 自定义镜像。
- sandbox、MCP、volume-manager 仍保持当前服务器禁用策略。
