# v003 私有市场源与模板兼容修复发版

## 范围

- 状态：成功
- 类型：混合
- 触发人：用户
- 分支：main / release/003
- 初始 main commit：2ba7e35
- FastGPT commit：dd91469
- release commit：以 `v003` tag 指向提交为准
- tag：v003
- 镜像 tag：fastgpt-custom:url-directory-003
- 回滚目标：服务器部署前 `fastgpt-custom:url-directory-002` 镜像和 `/opt/fastgpt/docker-compose.server.override.yml.bak.20260607164600`

## 变更

- FastGPT 本地二开入口控制：隐藏当前私有部署不可用或依赖收费/外部授权/重资源组件的原 FastGPT 入口。
- 新增/调整私有市场源能力，支持在当前项目内使用本地初始化的模板/工具市场源。
- 修复从模板市场、创建页、模板创建面板导入旧 workflow 模板时的兼容问题。
- 兼容旧模板中的非法 `valueType`、旧版输出 `type` 数组、缺失 `renderTypeList`、缺失变量 `description` 和不完整 `enums`。
- 增加纯工具测试跳过 MongoMemoryServer 的测试开关 `FASTGPT_TEST_SKIP_MONGO=1`。
- 项目规则增加 `发版` 口令：提交当前相关改动、执行版本流程并部署至服务器。

## 验证

- FastGPT：`FASTGPT_TEST_SKIP_MONGO=1 pnpm --filter @fastgpt/app test test/pageComponents/dashboard/agent/utils/appTemplateParse.test.ts` 通过，18 tests passed。
- FastGPT：`pnpm --filter @fastgpt/app typecheck` 通过。
- FastGPT：`git diff --check` 通过，仅有 Windows 换行提示。
- ai-knowledge-base：`git diff --check` 通过，仅有 Windows 换行提示。
- Docker：`docker build -f projects/app/Dockerfile -t fastgpt-custom:url-directory-003 .` 成功。
- 服务器：`docker compose --env-file .env -f docker-compose.pg.yml -f docker-compose.server.override.yml config` 通过。

## 服务器记录

- 快照：未单独确认腾讯云快照；本次回滚点为部署前 compose override 备份和上一版镜像 `fastgpt-custom:url-directory-002`。
- 部署前资源状态：内存 3.6GiB，available 约 1.4GiB；swap 2.0GiB，可用约 1.7GiB；根分区 59G，剩余 36G。
- 部署前容器状态：`fastgpt-app fastgpt-custom:url-directory-002 Up 20 hours`；FastGPT 依赖容器和 Caddy 均正常。
- 部署命令：
  - 本地构建：`docker build -f projects/app/Dockerfile -t fastgpt-custom:url-directory-003 .`
  - 本地导出：`docker save fastgpt-custom:url-directory-003 -o fastgpt-custom-url-directory-003.tar`
  - 上传镜像：`scp -C fastgpt-custom-url-directory-003.tar kb-server:/tmp/fastgpt-custom-url-directory-003.tar`
  - 上传 override：`scp deploy/server/docker-compose.server.override.yml kb-server:/tmp/docker-compose.server.override.yml`
  - 服务器加载：`sudo -n docker load -i /tmp/fastgpt-custom-url-directory-003.tar`
  - override 备份：`/opt/fastgpt/docker-compose.server.override.yml.bak.20260607164600`
  - compose 校验：`sudo -n docker compose --env-file .env -f docker-compose.pg.yml -f docker-compose.server.override.yml config`
  - 重启 app：`sudo -n docker compose --env-file .env -f docker-compose.pg.yml -f docker-compose.server.override.yml up -d fastgpt-app`
- 部署结果：`fastgpt-app` 已使用 `fastgpt-custom:url-directory-003` 启动，未重启依赖容器。
- 访问验证：`http://127.0.0.1:3000` 返回 200；`https://kb.zero007.chat` 返回 200；`/api/common/system/getInitData` 返回 `code:200`。
- 部署后资源状态：`fastgpt-app` 稳定后约 577MiB / 900MiB；内存 available 约 1.6GiB；swap 可用约 1.6GiB。
- 日志检查：FastGPT 已完成系统初始化，MongoDB/Postgres/队列 worker 正常。保留已知提示：Redis 当前为 `allkeys-lru`，sandbox 禁用导致启动检查出现 `fastgpt-code-sandbox` warning。启动期出现一批 `Failed to sync MongoDB indexes` 超时日志，之后服务访问和 API 正常，08:47:10Z 后未再出现新的 fatal/Mongo index/sandbox 报错。
- 临时文件清理：本地和服务器 `/tmp/fastgpt-custom-url-directory-003.tar` 已删除，服务器 `/tmp/docker-compose.server.override.yml` 已删除。

## 备注

- 本版本仅部署 FastGPT app 自定义镜像。
- 模型渠道、Embedding 模型、Base URL 和 API Key 继续只通过 FastGPT 原有页面配置。
- 本次 tar 上传耗时较长。后续建议将发版链路升级为 GitHub Actions 构建固定 tag 镜像并推送到镜像仓库，服务器通过 `docker pull` 部署，避免本地 `docker save` + `scp` 大文件传输。
- FastGPT 源码提交保留在本地 `main`，当前远端是官方 upstream，不推送到 `labring/FastGPT`。
